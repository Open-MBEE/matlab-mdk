classdef LoginResponse
    properties
    end
    methods(Static)
        function out = construct()
        %% construct
        %% Description: Construct a struct that represents the LoginResponse model with keys defined and defaults populated.
            out = struct();
            out.data = [];
        end
    end
end


