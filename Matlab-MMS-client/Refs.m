classdef Refs
    properties
    end
    methods(Static)
        function out = construct()
        %% construct
        %% Description: Construct a struct that represents the Refs model with keys defined and defaults populated.
            out = struct();
            out.refs = [];
        end
    end
end


