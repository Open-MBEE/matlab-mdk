classdef Groups
    properties
    end
    methods(Static)
        function out = construct()
        %% construct
        %% Description: Construct a struct that represents the Groups model with keys defined and defaults populated.
            out = struct();
            out.groups = [];
        end
    end
end


